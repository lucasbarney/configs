## `@lucasbarney/prettier-configs`

- Usage `Yarn` pkg:

```
$ yarn add @lucasbarney/prettier-configs -D
```

- Add to `.prettierrc.js` (project root folder)

```
const configs = require('@lucasbarney/prettier-configs');

module.exports = configs;
```
